<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="@dimen/global_fifty_dp"
    android:background="@color/toolbar_bg_blue"
    android:minHeight="?attr/actionBarSize"
    android:transitionName="toolbar"
    app:contentInsetLeft="0dp"
    app:contentInsetStart="0dp"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

    <!--android:contentInsetLeft="0dp"-->
    <!--android:contentInsetStart="0dp"-->

    <ImageView
        android:id="@+id/TITLE_back_btn"
        android:layout_width="@dimen/global_fifty_dp"
        android:layout_height="@dimen/global_fifty_dp"
        android:layout_gravity="start"
        android:scaleType="centerInside"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_back_arrow"
        android:visibility="visible" />

<!--    <ImageView
        android:id="@+id/TITLE_image"
        android:layout_width="@dimen/global_one_seventy_dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:src="@drawable/img_toolbar_title_image1"
        android:visibility="visible" />-->

    <TextView
        android:id="@+id/TITLE_text"
        android:layout_width="170dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:singleLine="true"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="@dimen/global_sixteen_sp"
        android:textStyle="bold"
        android:visibility="gone"
        />

    <TextView
        android:id="@+id/TITLE_btn_txt"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:gravity="center_vertical"
        android:padding="@dimen/global_ten_dp"
        android:singleLine="true"
        android:text="Button"
        android:textColor="@android:color/white"
        android:textSize="@dimen/global_sixteen_sp"
        android:visibility="gone"
        android:soundEffectsEnabled="false"/>

<!--    <ImageView
        android:id="@+id/TITLE_close_img"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:gravity="center_vertical"
        android:padding="@dimen/global_fifteen_dp"
        android:visibility="visible"
        android:soundEffectsEnabled="false"
        android:src="@drawable/ic_close"/>-->

</android.support.v7.widget.Toolbar>